In the following $HOME is your home directory.

+ The init.lua file should be placed into $HOME/.config/nvim/init.lua
+ .tmux.conf is places in $HOME
+ .vimrc is placed in $HOME

To install neovim locally run you can run the bash script localnviminstall.sh
